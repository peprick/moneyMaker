# React Frontend

Vite React app for the Quant Portfolio Optimizer.

## Run

```powershell
cd C:\Users\Sagarnil\new_proj\frontend
npm.cmd run dev
```

The dev server proxies `/api` calls to:

```text
http://127.0.0.1:8081
```

Start the Python quant engine and Spring Boot backend first.

